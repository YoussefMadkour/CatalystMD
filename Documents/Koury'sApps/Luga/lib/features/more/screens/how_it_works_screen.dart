import 'package:flutter/material.dart';

class UhowUitUworksScreen extends StatelessWidget {
  const UhowUitUworksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('how it works')),
      body: const Center(child: Text('UhowUitUworks placeholder')),
    );
  }
}
