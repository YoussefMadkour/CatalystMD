import 'package:flutter/material.dart';

class UnotificationsScreen extends StatelessWidget {
  const UnotificationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('notifications')),
      body: const Center(child: Text('Unotifications placeholder')),
    );
  }
}
