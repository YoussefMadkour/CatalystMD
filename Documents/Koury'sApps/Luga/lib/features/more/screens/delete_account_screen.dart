import 'package:flutter/material.dart';

class UdeleteUaccountScreen extends StatelessWidget {
  const UdeleteUaccountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('delete account')),
      body: const Center(child: Text('UdeleteUaccount placeholder')),
    );
  }
}
