import 'package:flutter/material.dart';

class UtrustUsafetyScreen extends StatelessWidget {
  const UtrustUsafetyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('trust safety')),
      body: const Center(child: Text('UtrustUsafety placeholder')),
    );
  }
}
